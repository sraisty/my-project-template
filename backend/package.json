{
  "name": "my-project-template-backend",
  "description": "handles dependencies across all the backend packages",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npm run test --prefix generic-backend-service && npm run test --prefix auth-service",
    "test:watch": "npm run test:watch --prefix generic-backend-service && npm run test:watch --prefix auth-service",
    "lint": "eslint --ext .ts "
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@eslint/compat": "^1.2.7",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.5",
    "@types/supertest": "^6.0.2",
    "dotenv": "^16.4.7",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.6",
    "ts-node": "^10.9.2"
  }
}